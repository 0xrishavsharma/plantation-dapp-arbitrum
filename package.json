{
  "name": "plantation-dapp-arbitrum",
  "license": "Apache-2.0",
  "version": "1.0.0",
  "scripts": {
    "build": "hardhat compile",
    "exec": "hardhat run scripts/deploy.js --network arbitrum"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.2",
    "ethers": "^5.1.2",
    "hardhat": "^2.2.0"
  },
  "dependencies": {
    "arb-shared-dependencies": "^1.0.0",
    "dotenv": "^8.2.0"
  }
}
